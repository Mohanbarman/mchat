export interface IToken {
    access_token: string;
}

export interface IUser {
    id: string;
    created_at: string;
    updated_at: string;
    name: string;
    email: string;
    profile: string;
    status: string;
}
